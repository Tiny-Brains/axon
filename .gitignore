# Nothing built here is committed: the binary comes from whatever builds the image, and the ONNX
# Runtime libraries `ort` downloads are large and platform-specific. `tests/fixtures/` IS committed
# -- `tests/fixtures/make-model.py` regenerates the models deterministically.
target/

# Local run artifacts
.run/
